@{
    ViewData["Title"] = "Запрос на восстановление пароля";
}
@model User

<h2>Введите логин.</h2>
<form asp-action="forgotpassword" asp-controller="authorization">
    <p>
        <label asp-for="Login">Логин</label>
        <input id="Login" type="text" asp-for="Login" required value="@Model?.Login" onblur="trimLogin()"/>
        <br/>
        <div hidden="@(!(Model != null && Model.Login == null))">Пользователь не найден.</div>
        <div hidden="@(!(Model != null && Model.Login != null && Model.Email == null))">Для указанного пользователя не задан Email для восстановления пароля.</div>
        
    </p>
    <p>
        <input type="submit" value="Запросить" />
    </p>
</form>
<a asp-controller="Authorization" asp-action="Login">Вход</a>
<script>
    function trimLogin() 
    {
        var login = document.getElementById("Login");
        login.value = login.value.trim();
    }
</script>